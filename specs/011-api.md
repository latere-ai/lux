---
title: "API: the /v1 kinds, addressing and concurrency, the error table, OpenAPI"
status: complete
track: core
depends_on:
  - specs/003-manifest-contract.md
  - specs/004-request-path.md
  - specs/006-identity.md
  - specs/007-keys-and-limits.md
  - specs/010-state.md
affects: [internal/api/, internal/serve/, internal/config/, api/, tools/apidoc/, docs/]
effort: large
created: 2026-09-13
updated: 2026-09-14
author: changkun
---

# API

## Overview

One HTTP surface under `/v1` on the public listener, one grammar for
every kind: apply by name, read by id or name, list, delete, and the
one verb a Key has. Every request body is a manifest
([[003-manifest-contract]]), every response body is JSON, and every
refusal is one envelope with one code and one fixed user sentence. This
spec owns that grammar, the addressing and the concurrency rules, the
usage and identity routes, the rate limit, and the error table, which
is the single table of every code the server can return on either
plane. The OpenAPI document is generated from the Go types and the
route table and served, so a generated client and the server agree by
construction rather than by review.

The grammar is small on purpose. Four kinds, four methods, one body
shape, one resolve. Everything a platform would want beyond it, roles,
teams, plans, a console, is the authorizer's and the platform's
([[001-architecture]], [[006-identity]]).

## Current state

`internal/api` is the surface below: the four kinds' four routes and
the Key's rotate, `/v1/self`, `/v1/openapi.json`, and
`/.well-known/lux`, over `manifest.Decode` and `manifest.Resolve`, the
identity of [[006-identity]], and the store of [[010-state]], with the
error table of both planes and the OpenAPI document generated from the
Go types by `tools/apidoc` into `api/openapi.yaml`. `cmd/luxd` mounts
it on the public listener in server mode and on the internal listener
in the file mode, beside the doors of [[004-request-path]], behind the
per-address bucket, with one metrics registry served at `/metrics`.
The two usage routes, `GET /v1/usage` and `GET /v1/requests`, are
mounted over the aggregation and the records of
[[009-usage-and-metering]], the second answering `source: memory`
until the archive of [[012-request-log-and-events]] lands. The doors
record through [[009-usage-and-metering]]'s `serve.Recorder`.

## Design

### The grammar

Four routes per kind, identical for `Provider`, `Model`, `Key`, and
`Budget`:

| Method | Path | Does |
|---|---|---|
| `PUT` | `/v1/{kind}s/{name}` | apply: create when no object of that name exists, update when one does; `201` on create, `200` on update; the body is a manifest, or a manifest without `apiVersion`, `kind`, and `metadata.name`, which the route supplies through `Decode`'s hint ([[003-manifest-contract]]); the response is the object `Put` wrote its `id`, `version`, `createdAt`, and `updatedAt` back into ([[010-state]]), so no second read follows the write |
| `GET` | `/v1/{kind}s/{id-or-name}` | read one object with its `status` |
| `GET` | `/v1/{kind}s` | list, with the filters and paging below |
| `DELETE` | `/v1/{kind}s/{id-or-name}` | delete; `204` with no body |

Apply is create-or-update because that is what one schema and one
resolve produce: the body is a whole object, `Resolve` runs the same
six stages whether or not `Existing` is set, and the response is the
resolved object either way. A caller that does not know whether the
object exists does not have to.

Apply is by name and only by name. A `PUT` whose path segment carries
an id prefix, `PUT /v1/keys/key_01J9...`, is `invalid_field` at
`metadata.name`, because no name may begin with one
([[003-manifest-contract]]) and the path is the name: a caller that
holds an id reads the object, which returns its name, and applies
that. There is therefore no creating `POST` on this surface and
nothing for an `Idempotency-Key` to guard: a `PUT` repeated with one
body is `201` then `200` with the same object, which is idempotency by
construction, and `If-None-Match: *` is the create-once precondition
for a caller that must not update. The one `POST` that changes state,
`/v1/keys/{id-or-name}/rotate`, is not idempotent and takes no key: a
repeat is a second rotation, and a caller whose response was lost
rotates again and holds the last value returned.

The path is versioned, `/v1`, and the body is versioned,
`lux.latere.ai/v1beta1`, on two axes. Under `/v1` every change is
additive: a route, a query parameter, a response member, an error code
may be added and none is removed or changed in meaning, which is the
same promise the schema makes for its fields
([[003-manifest-contract]]). A change that cannot be additive is
`/v2`, served beside `/v1` for the period the CHANGELOG names, and is
independent of the manifest's `apiVersion`, which moves on its own
rule: a `/v1` route may serve a `lux.latere.ai/v2` body one day, and
`/v2` may still serve `v1beta1`.

There is no `PATCH`, in any mode. A partial body cannot go through
`Resolve`: stages 1 and 2 validate and default a whole object, stage 5
compares a whole object against `Existing`, and a merge would need a
second set of rules for absent versus null, for list members, and for
maps, living beside the schema and drifting from it. That is a second
meaning for a manifest, which invariant 1 of [[001-architecture]]
forbids. The loop is `GET`, edit, `PUT`, and it closes because `status`
in an applied manifest is ignored ([[003-manifest-contract]]), so the
body a `GET` returned re-applies unchanged. A `PATCH` on a route in the
table is `not_found`, the same answer [[004-request-path]] gives a
method the door table does not list.

### Addressing

An object is applied by name and addressed by either. `{id-or-name}` is
the kind's prefixed ULID (`prv_`, `mdl_`, `key_`, `bud_`) when it
carries that prefix and a name otherwise, resolved through
`Objects.Get` or `Objects.ByName` ([[010-state]]); the two never
collide because a name may not begin with a prefix
([[003-manifest-contract]]), and an id of another kind's prefix on a
route, `/v1/keys/prv_...`, is `not_found`. A body whose
`metadata.name` differs from the path is `invalid_field` at
`metadata.name`; a body with no `metadata.name` takes the path's.

`/v1/models` on this surface is the `Model` kind: the operator's and
the platform's view, bearer-authenticated, in the manifest shape. The
model list an SDK asks for, `GET /openai/v1/models`, `GET
/anthropic/v1/models`, `GET /gemini/v1beta/models`, and `GET
/lux/v1/models`, is a door route, Key-authenticated, in that dialect's
own list shape, and is [[004-request-path]]'s. The paths differ by the
door prefix, so the two never meet in this server; a platform that
mounts a door at its own origin's root, so that an SDK's default
`/v1/models` reaches it, chooses there which of the two answers that
path, and this spec takes no position on its choice.

A `Model` name may carry `/`, because a discovered Model is named
`<provider>/<upstream name>` and the upstream name may itself contain
`/` ([[003-manifest-contract]]). The model routes are therefore
`/v1/models/{name...}`, matching any number of segments, and the
handler unescapes each segment and rejoins them with `/`. Both
`/v1/models/openai%2Fgpt-5` and `/v1/models/openai/gpt-5` name the same
object, and `mdl_` ids always work; a rejoined name that fails 003's
name rule is `not_found`.

The other three kinds use `{name}`, one segment. Their names are
DNS-1123 labels, which cannot contain `/`, and `keys` carries a
subroute, so a trailing wildcard there would swallow `/rotate`. The
asymmetry is deliberate and is the only one.

A path that is not clean, one with a repeated slash, a dot segment, or
a trailing slash, is `not_found` before any route is matched, because
the standard mux would otherwise answer it with a redirect to the clean
path, and a redirect is not an answer in the table. A `PUT` of a
`Model` whose name a discovered Model holds is a create: the discovered
Model does not count as `Existing`, `Resolve` runs against nothing, the
store replaces the row in place keeping its id ([[010-state]]), the
action is `model.create`, the answer is `201`, and the event is
`model.created`, because the declared Model is the first one an
operator wrote under that name.

### Concurrency

Every object response carries `ETag: "<version>"` and the same integer
in `status.version`, from the store's `version` for that row
([[010-state]]). `status.version` is rendered by the API and is not
part of the manifest a caller writes: like the rest of `status` it is
ignored on apply, so a read-modify-write round trip carries it back
harmlessly.

| Request header | Means | On a mismatch |
|---|---|---|
| `If-Match: "<version>"` | update this object at exactly this version | `conflict`, 409 |
| `If-Match: *` | the object must exist | `not_found`, 404 |
| `If-None-Match: *` | create only; the name must be free | `already_exists`, 409 |
| none | read-modify-write | retried, below |

A `PUT` without a precondition reads the current object, resolves
against it, and writes with the version it read; a `ErrVersionConflict`
from the store is retried up to three times before `conflict` is
returned, so a caller that does not care about concurrency does not see
one. A create whose name was taken between the read and the write is
`ErrNameTaken`, which is `already_exists`. `DELETE` and the rotate accept `If-Match`
with the same meaning, and `If-None-Match` on either is `invalid_field`
at the header, since neither creates. The only forms accepted are `*`
and one quoted decimal integer above zero; a weak validator (`W/"7"`),
a list, an unquoted number, the header sent twice, or both headers
together is `invalid_field` at `If-Match` or `If-None-Match`, both when
both were sent, never a silent match. The version is the row's and starts at `1` for every
row, so a name deleted and created again starts again at `1`; an
`ETag` held across a delete is a stale precondition like any other.

### The route table

Every route under `/v1` requires a bearer from a listed issuer
([[006-identity]]) and asks the authorizer the action in its row,
except the rows whose action is `none`: `/v1/self` and the tunnel
carrier are authenticated and authorize nothing, the carrier because
the session it joins was authorized at connect
([[013-tunnelled-runtimes]]); `/v1/openapi.json` and `/.well-known/lux`
need no bearer at all, because a document that describes the API
carries no installation's data; and `/internal/tunnel/{id}` is not
under `/v1`, takes `LUX_TUNNEL_FORWARD_SECRET`, and is on the internal
listener alone.

| Method | Path | Does | Action |
|---|---|---|---|
| `PUT` | `/v1/providers/{name}` | apply | `provider.create` or `provider.update` |
| `GET` | `/v1/providers` | list | `provider.list` |
| `GET` | `/v1/providers/{id-or-name}` | read, never with a credential value | `provider.read` |
| `DELETE` | `/v1/providers/{id-or-name}` | delete; `provider_in_use` while a declared Model targets it; its discovered Models go with it ([[005-providers]]) | `provider.delete` |
| `POST` | `/v1/providers/{id-or-name}/tunnel` | the tunnel session of a `tunnel: true` Provider ([[013-tunnelled-runtimes]]) | `provider.tunnel` |
| `POST` | `/v1/providers/{id-or-name}/tunnel/carry` | one carrier stream of an open session ([[013-tunnelled-runtimes]]) | none: the session is the authorization |
| `POST` | `/internal/tunnel/{id}` | internal listener only: one request forwarded from another replica, bearer `LUX_TUNNEL_FORWARD_SECRET` ([[013-tunnelled-runtimes]]) | none |
| `PUT` | `/v1/models/{name...}` | apply a declared Model, replacing a discovered one of that name | `model.create` or `model.update` |
| `GET` | `/v1/models` | list; `?source=declared\|discovered`, `?provider=` | `model.list` |
| `GET` | `/v1/models/{name...}` | read | `model.read` |
| `DELETE` | `/v1/models/{name...}` | delete; a declared Model's deletion lets discovery restore the discovered one | `model.delete` |
| `PUT` | `/v1/keys/{name}` | apply; a create returns `status.value` once, unless the body supplied `spec.value` ([[007-keys-and-limits]]), in which case the response carries neither `spec.value` nor `status.value` and `status.prefix` is the `sup_` handle | `key.create` or `key.update` |
| `GET` | `/v1/keys` | list, never with a value | `key.list` |
| `GET` | `/v1/keys/{id-or-name}` | read with `status.usage` | `key.read` |
| `DELETE` | `/v1/keys/{id-or-name}` | delete; the hash is removed in the same transaction | `key.delete` |
| `POST` | `/v1/keys/{id-or-name}/rotate` | mint a new value, keep the id, name, spec, owner, and windows; `200` with `status.value` | `key.update` |
| `PUT` | `/v1/budgets/{name}` | apply | `budget.create` or `budget.update` |
| `GET` | `/v1/budgets` | list | `budget.list` |
| `GET` | `/v1/budgets/{id-or-name}` | read with `status.spent` and `status.resetsAt` | `budget.read` |
| `DELETE` | `/v1/budgets/{id-or-name}` | delete; `budget_in_use` while a Key names it ([[007-keys-and-limits]]) | `budget.delete` |
| `GET` | `/v1/usage` | aggregates, `{"items": [rows]}`; the parameters and the row shape are [[009-usage-and-metering]]'s | `usage.read` |
| `GET` | `/v1/requests` | records, newest first, `{"items": [records], "next_cursor": "...", "source": "archive\|memory"}`; the parameters, the record, `source`, and the `limit` cap of 1000 are [[009-usage-and-metering]]'s | `usage.read` |
| `GET` | `/v1/self` | the caller's identity and what this server holds for it, below | none |
| `GET` | `/v1/openapi.json` | the served OpenAPI document; no bearer | none |
| `GET` | `/.well-known/lux` | the server's identity, below; no bearer | none |

The handler order on every item route is read, authorize, act: load the
object by id or name, build the authorizer's `resource` from what was
loaded, ask, then act. A missing object is `not_found`; a deny on the
request's own action is `forbidden`; a deny reached through `Lookup`
during `Resolve` is `not_found` ([[006-identity]]). `?owner=` and every
other list selector is intersected with the authorizer's `filter`, so a
selector outside the filter is an empty list and never a 403.

`GET /v1/self` asks no action, because asking permission to see the
identity the caller just presented is a category error: the response
carries what the caller sent and what this server was already told
about it.

```json
{
  "subject": "https://login.example.com|alice",
  "issuer": "https://login.example.com",
  "sub": "alice",
  "claims": {"email": "alice@example.com", "name": "Alice", "groups": ["research"]},
  "policy": "authorizer",
  "limits": {"requests_per_minute": 1200, "max_key_spend": "50"},
  "filter": {"owners": ["https://login.example.com|alice"]}
}
```

`policy` is `authorizer`, `owner`, or `file`. `limits` and `filter` are
what this replica remembers of the subject's last allow, kept by the
API for the allow's `ttl`, and are absent until a decision has been
made for it on this replica, which the field's documentation says;
they are a report of what was granted, never a grant. The API keeps
this memo itself, at most 65 536 subjects, because the shared client's
cache is keyed by subject, action, and resource id and cannot be read
by subject alone ([[006-identity]]); the same memo is where the rate
bucket below reads `requests_per_minute` from. `limits` carries the
wire names of the limits object, `requests_per_minute`,
`max_key_requests_per_minute`, `max_key_tokens_per_minute`,
`max_key_spend`, `max_key_ttl`, and `max_keys`, each absent when the
allow did not name it.

The struct that renders them, `SelfLimits`, is the one declaration of
those six wire names outside `authorizer/`
([[022-authorizer-vocabulary-package]]), and is exempted by name in
that spec's `TestVocabularyHasOneHome` with the reason: this is a
response schema of the `/v1` API, in the OpenAPI document as every
other schema is, reporting what the last allow granted. It reads no
authorizer's answer and is not a second reading of the contract, which
is `authorizer.WireLimits`.

`GET /.well-known/lux` is the one document a client reads before it has
a token, so a command, a platform's front, and the conformance suite
configure themselves from one fetch:

```json
{
  "name": "lux",
  "version": "0.1.0",
  "apiVersion": "lux.latere.ai/v1beta1",
  "api": "https://lux.example.com/v1",
  "openapi": "https://lux.example.com/v1/openapi.json",
  "doors": {
    "openai": "https://lux.example.com/openai",
    "anthropic": "https://lux.example.com/anthropic",
    "gemini": "https://lux.example.com/gemini",
    "lux": "https://lux.example.com/lux"
  },
  "dialects": ["openai", "anthropic", "gemini", "lux"],
  "issuers": ["https://login.example.com"],
  "audience": "lux",
  "mode": "server"
}
```

Every URL is built from `LUX_PUBLIC_URL`. `mode` is `server` or
`file`; in the file mode `issuers` is empty and `audience` blank, and
`api` and `openapi` still name the public address, since the document
has no other, while `mode` tells the reader the surface is on the
internal listener. The document names the build and the issuers and
nothing else; an issuer URL is public by definition, and the version of
an open source gateway is not a secret an installation can keep.

### The lifecycle of an apply

```mermaid
sequenceDiagram
  participant C as caller
  participant A as /v1 handler
  participant I as identity
  participant Z as authorizer
  participant M as manifest.Resolve
  participant S as store
  participant J as journal
  C->>A: PUT /v1/keys/run-42, If-Match "7"
  A->>A: request id, body limit
  A->>I: verify the bearer against the issuers
  I-->>A: subject, claims
  A->>A: the subject's rate bucket, the preconditions' syntax
  A->>M: Decode with the route's hint
  A->>S: ByName(Key, run-42)
  S-->>A: the object and version 7
  A->>Z: key.update on the loaded object
  Z-->>A: allow, limits
  A->>A: the preconditions against version 7
  A->>M: Resolve with Actor, Lookup, Defaults, Limits, Existing
  M->>Z: model.use per selector, budget.draw
  Z-->>M: allow or deny; a deny is not_found
  M-->>A: the resolved object and its warnings
  A->>S: Put(obj, ifVersion 7), the hash or the credential, and the journal row, one transaction
  S->>J: key.updated
  S-->>A: version 8
  A-->>C: 200, the resolved object, ETag "8", Lux-Request-Id
```

A refusal at any step is the envelope below, and nothing after it runs.
`Decode` runs before the read and the authorizer because a create's
`resource` is built from the body ([[006-identity]]) and a body that
does not decode should cost no store read; it reads no store and leaks
nothing. The authorizer is asked before `Resolve` for the request's own
action and during `Resolve` for the references, which is why a manifest
naming a Budget the caller may not draw is `not_found` on the field
rather than `forbidden` on the request. The preconditions are checked
after the authorizer answers, so whether a name is taken is learnt by a
caller the authorizer allowed and no other.

### Assembling Resolve

`manifest.Options` ([[003-manifest-contract]]) is built per request:

| Field | From |
|---|---|
| `Actor.Subject` | the rendered subject of the verified bearer ([[006-identity]]) |
| `Lookup` | the store and the authorizer's decision cache, asking `provider.read` per target, `budget.draw`, and `model.use` per selector, the last never cached because a selector is not an id ([[006-identity]]), mapping a deny to `not_found` and an unavailability to `authorizer_unavailable` |
| `Defaults` | `LUX_DEFAULT_REQUESTS_PER_MINUTE`, `LUX_DEFAULT_TOKENS_PER_MINUTE`, `LUX_UPSTREAM_TIMEOUT` |
| `Limits` | the `limits` of the decision on the request's own action ([[006-identity]]) |
| `Existing` | the object loaded by name, `nil` on a create |
| `FileMode` | false; the file mode never applies through the API ([[010-state]]) |
| `AllowPrivateUpstreams` | `LUX_UPSTREAM_ALLOW_PRIVATE` |
| `PublicURL` | `LUX_PUBLIC_URL` |
| `Now`, `NewName` | the clock and the API's name generator |

`max_keys` from the decision is checked at `key.create` in the same
transaction as the write, and a create over it is `ceiling_exceeded`
([[006-identity]]). The response is the resolved object with the
stored status merged in, which is what [[001-architecture]]'s
`TestAPIAndImporterResolveAgree` compares against `Resolve`'s own
output.

`Resolve` returns an object with an empty `status` but for what it
fills, and the API fills the rest before `Put`: `status.id`, a fresh
prefixed ULID on a create and the existing object's on an update;
`status.owner`, the actor on a create and the existing owner on an
update; `status.createdAt`, the existing object's on an update. For a
Key, `status.budget` names the Budget the `Lookup` resolved by name and
id, `status.prefix` is kept on an update and computed from the value on
a create, and the value, minted through `serve.MintKeyValue` or
supplied in `spec.value`, is hashed, registered through `Keys.Put` in
the transaction, and taken out of the object; a minted value is set on
the response after the row is written and nowhere else. For a
Provider, a value in `spec.credential.value` is sealed under the first
key of `LUX_SECRETS_KEK` and written through `Credentials.Put` in the
transaction with `status.credential` `{set: true, version: n+1,
updatedAt}`; a body without a value keeps the stored credential and
its status whether or not it carries the credential block, because
`Resolve` fills the block for every Provider that is not tunnelled and
[[003-manifest-contract]]'s update rule says an absent value keeps the
stored one, so a manifest read back re-applies without touching the
secret, and only a delete removes the row; a Provider that never had a
value reads `{set: false}`. A Key's `status.state` and `status.usage`
and a Budget's read-time members are rendered through
[[007-keys-and-limits]]'s `RenderKey` and `RenderBudget` on every
response that carries the object, an apply's included, so a create
already reads `Active`.

Every mutation journals its event of [[012-request-log-and-events]]
inside the transaction that writes the object, through
`serve.AppendEvent`, with `reason: request`, the subject, and the
request id: `<kind>.created` with the row's `data`, `<kind>.updated`
with the changed paths, computed over the JSON of `metadata` and
`spec` before and after, so a default `Resolve` derives from a changed
field, a Model's cache prices from `input`, counts as changed;
`<kind>.deleted`, a Key's with its prefix; and `key.rotated` with both
prefixes.

### Bodies, types, and limits

A request body is a manifest in one of the content types
[[003-manifest-contract]] decodes: `application/json`,
`application/yaml`, `application/x-yaml`, `text/yaml`. Anything else is
`unsupported_media_type`. A body above `LUX_MAX_MANIFEST_BYTES`
(default `65536`) is `body_too_large`, read no further; the data plane
has its own, larger limit ([[004-request-path]]).

Every response body is JSON, in the Go struct order the OpenAPI
document records. There is no content negotiation: a manifest is
written in YAML by people and read back by programs, and the `lux`
command renders YAML locally from the JSON it received
([[014-agent-client]]), so a second encoder on the server would buy one
convenience at the cost of a second golden corpus and a second set of
round-trip tests.

A list response is `{"items": [...], "next_cursor": "<cursor>"}`, with
`next_cursor` absent on the last page. The member is spelled as the API
grammar below spells it, so a client written for one surface pages every
other the same way.

### Lists

| Parameter | Kinds | Rule |
|---|---|---|
| `label` | every | `k=v`, repeatable; every pair must match |
| `owner` | every | a rendered subject; intersected with the authorizer's `filter` |
| `source` | models | `declared` or `discovered` |
| `provider` | models | a Provider name or `prv_` id; Models with a target on it |
| `limit` | every | default `50`, at most `200`; above is `invalid_field` |
| `cursor` | every | the previous response's `next_cursor`, opaque |

The parameters map onto `Objects.List`'s `Filter` and `Page`
([[010-state]]). A cursor is the store's, opaque to the caller, and
valid only for the kind and filter that produced it; anything else is
`invalid_field` at `cursor`. A query parameter no route defines, and
`source` or `provider` on a kind other than `Model`, is `invalid_field`
at that parameter's name, the same strictness the manifest has for a
field, so a misspelt filter is a refusal and not a silently wider list.
Two values for one `label` key match nothing and are an empty list. A
list never returns an object the authorizer's `filter` excludes, and
never returns a Key's value or a Provider's credential. The filter's
`labels` join the caller's as one conjunction, and a pair the two
disagree on is an empty list; one owner in the filter goes to the store
as `Filter.Owner`, and several owners are applied to the store's pages
by the API, which reads pages until `limit` items are kept or the rows
end, so a page under several owners may be shorter than `limit` while
`next_cursor` is still the store's.

### The usage routes

`GET /v1/usage` and `GET /v1/requests` are this spec's routes over
[[009-usage-and-metering]]'s parameters, rows, and records, mounted in
`internal/api/usage.go` and listed in the OpenAPI document. The seam
between the two specs is exact:

- The route parses the parameters as that spec defines them, refusing
  a bad one with `invalid_field` at its name, and builds
  `metering.Query` or `metering.RecordQuery`.
- The `key`, `model`, and `provider` parameters take a name or an id;
  the route resolves each name to its id through `Objects.ByName`
  before the query and before the authorizer, so `Query.Keys`,
  `.Models`, `.Providers` carry ids alone and the `usage.read`
  resource of [[006-identity]] carries `keys` as ids and `owners` as
  rendered subjects. A name that resolves to nothing is an empty
  result, never `not_found`, for the same reason a filter outside the
  authorizer's is.
- The authorizer's `filter` is intersected with the query: `owners`
  narrows `Query.Owners`, `labels` narrows `Query.Labels`; an
  intersection that is empty is an empty `items`, never a 403.
- `/v1/usage` is not paged: it answers `{"items": [rows]}` and the 90
  day range bound is what keeps the row count finite. `/v1/requests`
  pages by `cursor` and `next_cursor` as a list does, with the `limit`
  default `50` and cap `1000` that spec sets, and carries `source`
  beside `items`. `source` is a member of the response and no
  parameter of the route: it reads `memory`, this replica's own ring,
  until the exporter of [[012-request-log-and-events]] makes the
  archive readable, and a query that names it is `invalid_field` at
  `source` like any parameter no route defines.

### The file mode

With `LUX_MANIFEST_DIR` set, desired state is the directory and no
caller can change it ([[010-state]]). The whole of `/v1`, the kinds,
the usage routes, `/v1/self`, and `/v1/openapi.json`, is mounted on the
internal listener and not on the public one, and needs no bearer,
because there is no issuer to require and nothing to authorize
([[006-identity]]); `/v1/*` on the public listener is `not_found`, the
answer of any path that is not mounted, and `/.well-known/lux` stays
on the public listener with `"mode": "file"`. Every `PUT`, `POST`, and
`DELETE` under `/v1/{kind}s`, the tunnel session and carrier included,
is `read_only`, 405, with `Allow: GET` and the developer detail naming
the directory; the refusal is before the body is read and before any
decode, so a large or malformed body in this mode costs nothing.
`PATCH` is `not_found` here as in every mode, because it is not a
route. `GET /v1/self` answers `"policy": "file"` with no subject, since
no bearer was presented. The read routes, `/v1/usage`, and
`/v1/requests` are served normally. It is one handler in both modes:
`cmd/luxd` mounts it on the internal listener and mounts its
`Unmounted` twin under `/v1` on the public one, which answers
`not_found` with a request id for every path; the mode is read off the
identity's `Policy` ([[006-identity]]), so a handler built over a file
mode store asks no bearer and no authorizer.

### The error table

The envelope is `latere.ai/x/pkg/httpjson`'s `ErrorEnvelope`, written
through `httpjson.WriteError`: one member `error` carrying `code`,
`message`, and `details`, the last a map. `details` always carries
`request_id`; it carries `paths`, a list of JSON paths, for a code that
names fields, and `detail`, one developer sentence, where there is one
to give. `message` is the fixed user sentence below and is never built
from an underlying error, an authorizer's `reason`, or an upstream's
body.

```json
{"error": {"code": "invalid_field", "message": "A field has a value it cannot take.",
           "details": {"request_id": "req_01J9...", "paths": ["spec.baseURL"],
                       "detail": "a loopback host needs LUX_UPSTREAM_ALLOW_PRIVATE"}}}
```

The table is both planes' in one place, because one code means one
thing whichever surface raised it and a caller that sees
`rate_limited` on a door and on `/v1` should read one sentence.
[[004-request-path]] renders these codes into each door's own shape;
the code, the status, and the sentence are the same. The table's scope
is every code a handler writes in an HTTP response on either plane.
Three groups of names are codes in their own specs and are not rows
here, because no response ever carries them as its error:
`client_closed`, the `error` a usage record carries when the caller
went away ([[004-request-path]], [[009-usage-and-metering]]); the
tunnel session's close reasons `superseded`, `token_expired`,
`provider_deleted`, `draining`, which travel in a frame
([[013-tunnelled-runtimes]]); and the `lux` command's own exit codes
([[014-agent-client]]).

| Code | Status | Message | Raised by |
|---|---|---|---|
| `malformed_body` | 400 | The request body is not valid JSON or YAML. | [[003-manifest-contract]] |
| `multi_document` | 400 | Send one manifest per request. | [[003-manifest-contract]] |
| `unsupported_version` | 400 | This server serves lux.latere.ai/v1beta1. | [[003-manifest-contract]] |
| `unsupported_kind` | 400 | This server does not serve that kind. | [[003-manifest-contract]] |
| `unknown_field` | 400 | The manifest has a field this schema does not know. | [[003-manifest-contract]] |
| `missing_field` | 400 | A required field is missing. | [[003-manifest-contract]] |
| `invalid_field` | 400 | A field has a value it cannot take. | [[003-manifest-contract]] |
| `reserved_prefix` | 400 | That name is reserved for the gateway. | [[003-manifest-contract]] |
| `exclusive_fields` | 400 | Two fields that cannot be set together are set. | [[003-manifest-contract]] |
| `duplicate_target` | 400 | Two targets name the same provider and model. | [[003-manifest-contract]] |
| `invalid_request` | 400 | The request body is not valid for this request. | [[004-request-path]], [[007-keys-and-limits]] |
| `upstream_rejected` | 400 | The provider rejected this request. | [[004-request-path]] |
| `dialect_unsupported` | 400 | This door cannot reach the provider that serves this model. | [[004-request-path]] |
| `provider_required` | 400 | Name a provider with the Lux-Provider header. | [[004-request-path]] |
| `currency_mismatch` | 400 | The budget and the model are priced in different currencies. | [[007-keys-and-limits]] |
| `unauthenticated` | 401 | This request needs a valid credential. | [[006-identity]], [[004-request-path]] |
| `forbidden` | 403 | You do not have permission to do this. | [[006-identity]] |
| `key_disabled` | 403 | This key is disabled. | [[007-keys-and-limits]] |
| `key_expired` | 403 | This key has expired. | [[007-keys-and-limits]] |
| `route_not_allowed` | 403 | This key may not use this route. | [[004-request-path]] |
| `model_not_allowed` | 403 | This key may not use that model. | [[004-request-path]] |
| `model_unpriced` | 403 | This model has no price, and this key spends under a limit. | [[007-keys-and-limits]] |
| `not_found` | 404 | There is no such object. | this spec, [[003-manifest-contract]], [[006-identity]] |
| `model_not_found` | 404 | There is no model of that name. | [[004-request-path]] |
| `read_only` | 405 | This server reads its manifests from a directory and cannot change them. | [[010-state]] |
| `already_exists` | 409 | An object of this kind already has that name. | this spec |
| `conflict` | 409 | The object changed since you read it; read it again and retry. | this spec, [[010-state]] |
| `immutable_field` | 409 | This field cannot be changed after the object is created. | [[003-manifest-contract]], [[007-keys-and-limits]] |
| `budget_in_use` | 409 | Keys still draw from this budget. | [[007-keys-and-limits]] |
| `provider_in_use` | 409 | Models still target this provider. | [[005-providers]] |
| `body_too_large` | 413 | The request body is larger than this server accepts. | [[004-request-path]], this spec |
| `unsupported_media_type` | 415 | Send the manifest as JSON or YAML. | [[003-manifest-contract]] |
| `ceiling_exceeded` | 422 | The value is above what you may ask for. | [[006-identity]], [[003-manifest-contract]] |
| `rate_limited` | 429 | Too many requests; wait and retry. | this spec, [[007-keys-and-limits]] |
| `spend_exceeded` | 429 | This key has spent its limit for the window. | [[007-keys-and-limits]] |
| `budget_exhausted` | 429 | The budget has nothing left for the window. | [[007-keys-and-limits]] |
| `internal` | 500 | Something went wrong on this server. | this spec |
| `upstream_error` | 502 | The provider returned an error. | [[004-request-path]] |
| `authorizer_unavailable` | 503 | The permission service is unavailable; retry shortly. | [[006-identity]] |
| `store_unavailable` | 503 | This server cannot reach its store; retry shortly. | this spec, [[010-state]], [[004-request-path]] |
| `provider_unavailable` | 503 | No provider for this model is available right now. | [[004-request-path]] |
| `upstream_timeout` | 504 | The provider did not answer in time. | [[004-request-path]] |

`internal` is the only code with no cause in another spec: it is what a
handler returns when it has a bug, a panic, a Key value that could not
be minted, or a credential that could not be sealed, and its `detail`
is empty because a caller learns nothing useful from a panic; the
developer's line, the panic and its stack with the request id, is the
log's. Every refusal on `/v1` also carries the code in `Lux-Error`, the
header the doors write ([[004-request-path]]), so a caller reading
headers alone reads one name on both planes; `Retry-After` on
`rate_limited` and `Allow: GET` on `read_only` are beside it. `store_unavailable` is any
store error that is not `ErrNotFound`, `ErrVersionConflict`,
`ErrNameTaken`, `ErrHashTaken`, `ErrInvalidCursor`, or `ErrReadOnly`
([[010-state]]), including one `Resolve` passes through from its
`Lookup` as a plain error ([[003-manifest-contract]]); `ErrInvalidCursor` is `invalid_field` at `cursor`, and
`ErrHashTaken` is `invalid_field` at `spec.value` with a detail that
names no Key ([[007-keys-and-limits]]).

`TestErrorTable` reads every spec in this deck, collects every code
named in an error table, the `Code` column of [[003-manifest-contract]]'s
and [[004-request-path]]'s tables and every backticked
`snake_case` word a spec says a request is "refused with" or
"answers", subtracts the three groups above, and asserts that each has
exactly one row here with a status and a sentence, that a code in
[[004-request-path]]'s table has the same status there, that no two
rows share a sentence, that every sentence is one sentence in the user
register, and that no handler on either plane writes a `message`
outside the table.

### Request ids

Every request has one, `req_` and a ULID, on every response in
`Lux-Request-Id`, in `details.request_id` of every error, in the
`request.id` the authorizer sees ([[006-identity]]), in the event's
`request_id` ([[012-request-log-and-events]]), on the usage record
([[009-usage-and-metering]]), and on the trace
([[019-observability]]). A caller's own `Lux-Request-Id` is ignored and
replaced, on both planes and for the same reason
([[004-request-path]]): an identifier a caller chooses is an identifier
a caller can reuse, and every one of the five places above assumes it
is unique.

A caller's `X-Request-Id`, when present, is echoed unchanged on the
response of both planes and used for nothing else: not as the record's
id, not in a log line, not toward a provider. It is a correlation
handle for the caller's own tracing, which is what the API grammar below
asks a surface to echo, and echoing it costs the server no assumption. A
value longer than 128 bytes or carrying a byte outside printable ASCII
is dropped rather than echoed, so a header cannot be used to smuggle
bytes into a response.

### Rate limits

After authentication, `LUX_REQUESTS_PER_MINUTE` per subject, which the
authorizer's `limits.requests_per_minute` overrides for that subject
([[006-identity]]); a token bucket per replica with the limit as the
burst, through `latere.ai/x/pkg/ratelimit`; `0` is no limit. The
override is read from the memo `/v1/self` reports, the last allow this
replica remembers for the subject, because the bucket runs right after
authentication and before the request's own decision: the request that
carries the first allow naming a rate is admitted under the configured
one, and every request after it under the subject's, for the allow's
`ttl`. Before authentication, one bucket per client address of
`LUX_UNAUTHENTICATED_REQUESTS_PER_MINUTE`, which bounds a flood of bad
tokens and, on the doors, of guessed Key values
([[007-keys-and-limits]]): a token is taken from the address's bucket
before the handler runs and given back after it unless the handler
answered `unauthenticated`, so the bucket counts refused credentials
and a caller behind one address who authenticates is never limited by
it, while an address whose bucket is empty is refused before the
verifier, the store, or the handler is touched, a valid credential
included, until the window refills. Both are per replica, so an
installation admits the configured rate times its replica count,
exactly as the data plane's do ([[009-usage-and-metering]]). The client address is the
peer's, or, when the peer is in `LUX_TRUSTED_PROXIES`, the last
`X-Forwarded-For` entry outside those ranges, so a gateway deployed
behind a platform's API layer limits per caller and not per proxy; a
forwarded header from any other peer is ignored, and the same address
is the authorizer's `request.ip` ([[006-identity]]). The bucket and the
address rule run in `internal/serve` before the door handler of
[[004-request-path]], which takes the address as a value and reads no
header for it.

Every `/v1` response past authentication carries `RateLimit-Limit`,
`RateLimit-Remaining`, and `RateLimit-Reset`, the last the seconds
until the bucket is full again; a `rate_limited` response adds
`Retry-After` in seconds and, from the address bucket, the three
headers of that bucket. A response refused before authentication and a
subject whose limit is `0` carry none, because there is no window to
describe.

### CORS

No `Access-Control-*` header is written on either plane, and an
`OPTIONS` request is `not_found`. The doors and `/v1` are API surfaces
for SDKs and for server-side callers, and the credential either takes,
a Key or an issuer token, is one a browser page should not hold. A
platform that wants a console puts it in front of this API with its own
origin, its own session, and its own CORS policy, which is the same
boundary every other platform concern sits on
([[020-building-a-plane]]).

### The API grammar

A platform that routes this surface under its own origin by path
prefix meets it with the grammar that origin holds every service to:
one bearer header, one error envelope, `limit` and `cursor` in and
`next_cursor` out, `/v1` additive, `Idempotency-Key` on a creating
`POST`, opaque identifiers, `429` with `Retry-After` and the
`RateLimit-*` headers, `X-Request-Id` echoed, and an OpenAPI document
published from code. This spec follows each rule where the core can
without learning a platform's name, and states its one deviation and
its two refinements so the platform's conformance test reads a decision
rather than a gap:

| Rule | Here |
|---|---|
| bearer | `Authorization: Bearer` on `/v1`; the audience is `LUX_OIDC_AUDIENCE` and a platform sets it to its own ([[006-identity]]) |
| envelope | `httpjson.ErrorEnvelope`, above, with the developer detail in `details.detail`; a `manifest.Error` maps as `Code.Message()` to `message`, `Paths` to `details.paths`, and `Detail` to `details.detail` ([[003-manifest-contract]]) |
| pagination | `limit`, `cursor`, `next_cursor`, opaque, above |
| versioning | `/v1` additive, above |
| idempotency | no creating `POST` exists; `PUT` by name is idempotent by construction, above. This is the deviation: the grammar's header has no route to apply to, and `POST .../rotate` is a mutation whose repeat is a second rotation |
| identifiers | prefixed ULIDs, opaque to a caller beyond the prefix. The grammar's owner spellings `me`, `org`, `u-<id>` are a platform's; here an owner is the rendered subject of [[006-identity]], and a platform renders it as it likes. This is the first refinement |
| limits | `429`, `Retry-After`, `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset`, below |
| tracing | `X-Request-Id` echoed, above; `Lux-Request-Id` is the server's own id beside it. This is the second refinement: the grammar's echo is honoured and the record's id stays the server's |
| reference | `api/openapi.yaml` from code, served at `GET /v1/openapi.json`, below |

The doors are outside the grammar by design: they speak each
provider's dialect verbatim so a third-party SDK drops in, which is the
exception the grammar itself makes for the compatibility dialects, and
[[004-request-path]] owns their shapes.

### The listeners

The public listener serves the four doors, `/v1`, `/.well-known/lux`,
and the three probes that a release smoke reaches through an ingress.
The two planes and `/.well-known/lux` sit behind one instance of the
per-address bucket, so a refused credential on either plane draws from
the same bucket; the probes and `/` sit outside it, because a health
check must answer whatever a caller's bucket says. The internal
listener serves the probes and `/metrics` ([[019-observability]]) and
no `/v1`, except in the file mode, where `/v1` is the internal
listener's alone. `/metrics` is never on the public listener
([[002-repository-scaffold]]). One `metrics.Registry` is built by
`cmd/luxd` for the process and handed to the store's `Instrument`, the
Key cache, the router, the doors, the authorizer's `Observe`, and this
surface, whose `lux_refusals_total{code}` counts every envelope
written; `/metrics` is that registry's exposition through the probes
handler.

### OpenAPI

The kinds' Go types carry no `apiVersion` or `kind` field, since
`MarshalJSON` writes both ([[003-manifest-contract]]); the generator
adds the two members to every kind's schema itself.

The generator lives in `internal/api` beside the route table and the
error table it reads, and `tools/apidoc` is the command that writes its
YAML to `api/openapi.yaml`: `go run ./tools/apidoc`. The handler serves
the same generation as JSON at `GET /v1/openapi.json`, rendered once at
start from the same code rather than converted from the committed YAML,
because `api/` holds no Go package under [[001-architecture]]'s package
rule and a package cannot embed a file outside its own tree; the one
source is the Go types, the route table, and the error table, and
`TestOpenAPIServedMatchesCommitted` holds the served JSON to the
committed YAML member for member. The document's objects keep the Go
struct order, so two generations are byte-identical. `TestOpenAPIIsCurrent`
fails when the committed document differs from a fresh generation,
which makes a route or a field added without regenerating a red build
rather than a silent drift. The write-only members, `spec.value` and
`spec.credential.value`, appear as `writeOnly` strings, `status` as
`readOnly`, every enum of `manifest/v1` with its values, and the error
table as `x-lux-errors` and as the `code` enum of the `Error` schema;
each operation names its action in `x-lux-action`. The conformance
suite validates every response it receives against the document
([[018-conformance-suite]]).

### Configuration

| Variable | Required | Default | Purpose |
|---|---|---|---|
| `LUX_PUBLIC_URL` | yes, in every mode | none | the absolute `http://` or `https://` URL callers reach the public listener at, with no query, fragment, or user information and its trailing slash dropped; the base of every URL in a response and of `/.well-known/lux`, and `Options.PublicURL` for the loop check of `Resolve` through the API and in the file mode alike, which is why the file mode requires it too |
| `LUX_REQUESTS_PER_MINUTE` | no | `600` | control plane requests one subject may send in a minute, per replica; `0` is no limit |
| `LUX_UNAUTHENTICATED_REQUESTS_PER_MINUTE` | no | `60` | requests one client address may send before authentication, per replica, on both planes |
| `LUX_TRUSTED_PROXIES` | no | unset | comma separated CIDR ranges of the proxies in front of the gateway; when the peer is in one, the client address is the last `X-Forwarded-For` entry outside every range; unset trusts no header, and the peer is the client |
| `LUX_MAX_MANIFEST_BYTES` | no | `65536` | the largest control plane request body accepted |

## Not in this spec

The schema, the decode, and the resolve stages
([[003-manifest-contract]]); the doors, their route table, the SDK's
model list `GET /{door}/v1/models`, and how a code is rendered per
dialect ([[004-request-path]]); who may do what
([[006-identity]]); the Key value, its rotation arithmetic, and the
windows ([[007-keys-and-limits]]); the usage parameters and the row and
record shapes ([[009-usage-and-metering]]); the store, the versions,
and the file mode's reading of a directory ([[010-state]]); the events
a mutation raises ([[012-request-log-and-events]]); the command that
speaks this API ([[014-agent-client]]); the suite that proves it
([[018-conformance-suite]]).

## Acceptance criteria

| Criterion | Test that proves it | State |
|---|---|---|
| Every route in the table exists and asks the authorizer exactly the action in its row; every row but `/v1/openapi.json` and `/.well-known/lux` is `unauthenticated` without a bearer, and those two serve without one; the four kinds' four routes are one table-driven case per kind | `TestRouteTableActions`, `TestGrammarPerKind`, table-driven over the four kinds and four methods | passing, `internal/api` |
| `PUT` twice with one manifest is 201 then 200 and the second response equals the first but for `status.version` and the timestamps | `TestApplyIsCreateThenUpdate` | passing, `internal/api` |
| A stale `If-Match` is `conflict`; `If-None-Match: *` on a taken name is `already_exists`; `If-Match: *` on a free name is `not_found`; a weak validator or a list is `invalid_field` at the header; without a precondition a concurrent write is retried and succeeds | `TestPreconditions`, `TestConcurrentApplyRetries` | passing, `internal/api` |
| `PUT /v1/keys/key_01J9...` is `invalid_field` at `metadata.name`; `GET /v1/keys/prv_01J9...` is `not_found`; a Key is read by its `key_` id and by its name and the two responses are equal | `TestApplyIsByNameOnly`, `TestAddressByIdOrName` | passing, `internal/api` |
| `PATCH` on every route in the table is `not_found` | `TestNoPatch` | passing, `internal/api` |
| A Model named `openai/gpt-5` is applied, read, listed, and deleted by its percent-encoded name, by its two-segment path, and by its id, all naming one object, and the three-segment `relay/anthropic/claude-sonnet-4` is one object too | `TestModelNamesWithSlashes` | passing, `internal/api` |
| A body whose `metadata.name` differs from the path is `invalid_field` at `metadata.name`; a body without one takes the path's | `TestNameOnApply` | passing, `internal/api` |
| `PUT /v1/keys/run-42` with a body of `{"spec": {"models": ["sonnet"]}}` creates the Key and answers the full envelope; the same body with `"kind": "Model"` is `unsupported_kind` | `TestApplyWithoutTheEnvelope` | passing, `internal/api` |
| `POST /v1/keys/{id}/rotate` returns a new `status.value`, keeps the id, name, spec, owner, and `status.usage`, and the old value is refused on a door | `TestRotate` with [[007-keys-and-limits]]'s `TestRevocationPropagates` | passing for the route, `TestRotate` in `internal/api`, the old hash gone and the new one registered; the door refusing the old value is [[007-keys-and-limits]]'s cache, passing there |
| `DELETE` of a Budget a Key names is `budget_in_use`, 409; after the Key is deleted the Budget deletes | `TestBudgetInUse` | passing, `internal/api` |
| A Key's value appears in the create and rotate responses and in no read, list, or OpenAPI example; a create with `spec.value` answers without `spec.value` and without `status.value` and with a `sup_` prefix; a Provider's credential appears in no response | `TestSecretsNeverInResponses`, `TestSuppliedValueIsNotEchoed` with [[007-keys-and-limits]]'s canary | passing, `internal/api` |
| A list pages through `items` and `next_cursor` at the default and the maximum with `next_cursor` absent on the last page, `?limit=201` is `invalid_field`, repeated `?label=` is a conjunction, `?owner=` outside the authorizer's `filter` is an empty list, a cursor from another kind is `invalid_field`, and an unknown query parameter is `invalid_field` at its name | `TestListSelectors`, `TestPagination`, `TestUnknownQueryParameter` | passing, `internal/api` |
| `GET /v1/usage?key=<name>` resolves the name to its id before the authorizer sees the resource, an unknown name is an empty `items`, the authorizer's `filter` intersects the query, and `GET /v1/requests` answers `items`, `next_cursor`, and `source` | `TestUsageRoutesResolveNamesAndIntersectFilter`, `TestUsageEnvelopes` | passing, `internal/api`, beside [[009-usage-and-metering]]'s `TestUsageQueryValidation`, `TestUsageFilterNarrows`, and `TestRequestsSource` there; `source` reads `memory` until [[012-request-log-and-events]]'s exporter, which that spec's row owns |
| Every code named as a refusal by any spec in this deck, less the three excluded groups, has exactly one row with a status and a unique one-sentence message; every code in [[004-request-path]]'s table has the same status here; and no handler on either plane writes a message outside the table | `TestErrorTable`, reading `specs/*.md` and the handler sources | passing, `internal/api`; the test also holds this spec's own table to the code row for row |
| A body that is not JSON, and one that is not YAML under a YAML type, is `malformed_body` with the line and column in `details.detail` | `TestMalformedBody` | passing, `internal/api` |
| The envelope carries `request_id` always, `paths` for every code that names a field, and an authorizer's `reason` only in `detail` | `TestEnvelope`, `TestDenyReasonStaysInDetail` | passing, `internal/api` |
| Every response, served or refused, carries `Lux-Request-Id`, and a caller's own is replaced; a caller's `X-Request-Id` of 128 printable bytes is echoed unchanged on both planes and appears in no record or log line, and one of 129 bytes or with a control byte is not echoed | `TestRequestIdOnEveryResponse`, `TestXRequestIdIsEchoedOnly` | passing, `internal/api` |
| In file mode every `PUT`, `POST`, and `DELETE` under `/v1/{kind}s` is `read_only` with `Allow: GET` before the body is read, `PATCH` is `not_found`, the read routes and the usage routes serve on the internal listener without a bearer, `/v1/*` on the public listener is `not_found`, and `/.well-known/lux` on the public listener says `"mode": "file"` | `TestFileModeIsReadOnly`, `TestFileModeMountsOnTheInternalListener` | passing, `internal/api` for the handler and `cmd/luxd`'s `TestFileModeMountsOnTheInternalListener` for the listeners |
| A body one byte over `LUX_MAX_MANIFEST_BYTES` is `body_too_large`; an unsupported content type is `unsupported_media_type`; a YAML and a JSON body of one manifest produce equal responses | `TestBodiesAndTypes` | passing, `internal/api` |
| The 601st authenticated request of one subject in a minute is `rate_limited` with the four headers; the 61st unauthenticated request from one address is; the authorizer's `requests_per_minute` overrides the first | `TestRateLimits` | passing: the subject bucket and the override in `internal/api`'s `TestRateLimits`; the address bucket in `internal/serve`'s `TestLimitUnauthenticatedCountsRefusedCredentials` and, across both planes, `cmd/luxd`'s `TestUnauthenticatedBucketSpansBothPlanes` |
| `GET /.well-known/lux` needs no bearer and names the build, the four doors built from `LUX_PUBLIC_URL`, the issuers, and the mode | `TestWellKnown` | passing, `internal/api` |
| `GET /v1/self` returns the caller's subject and claims, `policy`, and the cached limits and filter, and asks the authorizer nothing | `TestSelf` | passing, `internal/api` |
| No response on either plane carries an `Access-Control-*` header and `OPTIONS` is `not_found` | `TestNoCORS` | passing, `internal/api` |
| The committed `api/openapi.yaml` equals a fresh generation, `GET /v1/openapi.json` is that document as JSON, and every response of the suite validates against it | `TestOpenAPIIsCurrent`, `TestOpenAPIServedMatchesCommitted` | passing for the two tests, `internal/api`; the suite's validation is [[018-conformance-suite]]'s |
| A store error that is not one of the four named errors is `store_unavailable`, and a handler panic is `internal` with an empty `detail` | `TestStoreErrorsMap` | passing, `internal/api` |
| Behind a peer in `LUX_TRUSTED_PROXIES` the per-address bucket and `request.ip` carry the last forwarded entry outside the ranges; a forwarded header from a peer outside them is ignored and the peer is the client; unset, every header is ignored | `TestTrustedProxies` | passing: the address rule in `internal/serve`'s `TestClientAddressRule`, `request.ip` and the bucket in `internal/api` |

## Outcome

Built on 2026-09-13 and 2026-09-14 in six commits on a branch merged to
`main` as `75dc283`, `1f5b227` through `56de1c1`, with the two usage
routes landing after in `63049e6` beside [[009-usage-and-metering]]. It
is proven by the whole gate and by coverage under the race detector of
93.7% for `internal/api`, 97.2% for `internal/serve`, and 96.1% for
`cmd/luxd`.

What was built: `internal/api`, the four kinds' four routes and the
Key's rotate over `manifest.Decode` and `manifest.Resolve`, the
addressing and the preconditions, the list selectors and paging, the
two usage routes, `/v1/self`, `/v1/openapi.json`, `/.well-known/lux`,
the per-subject rate bucket, the error table with `writeError` as its
one writer, and the OpenAPI generator that `tools/apidoc` writes to
`api/openapi.yaml` and the handler serves as JSON;
`internal/config`'s control plane rows with `LUX_PUBLIC_URL` required
in every mode; `internal/serve`'s client address rule, the
per-address bucket, and the seams the surface mounts through; and
`cmd/luxd`'s two listeners, which mount the doors and `/v1` per mode
behind one bucket with one metrics registry. The Design above was
rewritten to what was built as this spec reached `testing`; what
diverged from the text as dispatched:

- The two usage routes were not mounted when the rest of the surface
  was, because the aggregation they read was being built at the same
  time. They mount now over [[009-usage-and-metering]]'s `serve.Usage`
  and `Store.Usage().Records`, and `source` reads `memory`, this
  replica's own ring, until the exporter of
  [[012-request-log-and-events]] makes the archive readable.
- The three tunnel rows of the route table, `POST
  /v1/providers/{id-or-name}/tunnel`, its carrier, and
  `/internal/tunnel/{id}`, are [[013-tunnelled-runtimes]]'s and are not
  mounted: `TestRouteTableActions` holds each to `not_found`, the
  answer of any path outside the table, and that spec mounts them and
  closes their half of the first criterion above.
- The API keeps its own memo of the last allow per subject rather than
  reading the shared client's cache, because that cache is keyed by
  subject, action, and resource id and cannot be read by subject
  alone; the memo is what `/v1/self` reports and what the rate bucket
  reads `requests_per_minute` from before the request's own decision is
  made. [[006-identity]]'s sentence on `/v1/self` was corrected to name
  it.
- `source` on `/v1/requests` is a member of the response and no
  parameter, so a query naming it is `invalid_field` at `source`; the
  route table's `archive\|memory` describes what the member carries,
  not a selector a caller sets.

What the neighbouring specs own from here:
[[013-tunnelled-runtimes]] mounts the three tunnel rows;
[[012-request-log-and-events]] makes `source` read `archive`;
[[018-conformance-suite]] validates every response it receives against
`api/openapi.yaml`; [[014-agent-client]] is the command that speaks
this surface; [[015-test-stubs-and-tiers]] runs the e2e tier over it.
Three rows of [[007-keys-and-limits]] and one of [[010-state]] that
waited for this surface were closed by its tests and their State cells
now name them.
