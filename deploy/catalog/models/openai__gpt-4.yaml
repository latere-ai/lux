apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: openai/gpt-4
spec:
  targets:
    - provider: openai
      model: gpt-4
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "10"
    output: "30"
