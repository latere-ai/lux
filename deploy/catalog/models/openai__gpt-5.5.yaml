apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: openai/gpt-5.5
spec:
  targets:
    - provider: openai
      model: gpt-5.5
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "5"
    output: "30"
    cachedInput: "0.5"
