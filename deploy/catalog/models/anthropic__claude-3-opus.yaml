apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: anthropic/claude-3-opus
spec:
  targets:
    - provider: anthropic
      model: claude-3-opus
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "15"
    output: "75"
    cachedInput: "1.5"
