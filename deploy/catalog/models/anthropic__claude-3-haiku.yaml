apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: anthropic/claude-3-haiku
spec:
  targets:
    - provider: anthropic
      model: claude-3-haiku
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "0.25"
    output: "1.25"
    cachedInput: "0.03"
