apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: zhipu/glm-4.5
spec:
  targets:
    - provider: zhipu
      model: glm-4.5
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "0.6"
    output: "2.2"
    cachedInput: "0.11"
