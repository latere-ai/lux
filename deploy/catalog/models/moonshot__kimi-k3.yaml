apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: moonshot/kimi-k3
spec:
  targets:
    - provider: moonshot
      model: kimi-k3
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "3"
    output: "15"
    cachedInput: "0.3"
