apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: moonshot/kimi-k2.5
spec:
  targets:
    - provider: moonshot
      model: kimi-k2.5
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "0.6"
    output: "3"
    cachedInput: "0.1"
