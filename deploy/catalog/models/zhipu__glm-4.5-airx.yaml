apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: zhipu/glm-4.5-airx
spec:
  targets:
    - provider: zhipu
      model: glm-4.5-airx
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "1.1"
    output: "4.5"
    cachedInput: "0.22"
