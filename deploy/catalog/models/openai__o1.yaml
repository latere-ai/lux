apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: openai/o1
spec:
  targets:
    - provider: openai
      model: o1
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "15"
    output: "60"
    cachedInput: "7.5"
