apiVersion: lux.latere.ai/v1beta1
kind: Model
metadata:
  name: zhipu/glm-4.6v
spec:
  targets:
    - provider: zhipu
      model: glm-4.6v
      weight: 100
      priority: 0
  fallback: never
  pricing:
    currency: USD
    per: 1000000
    input: "0.3"
    output: "0.9"
    cachedInput: "0.05"
