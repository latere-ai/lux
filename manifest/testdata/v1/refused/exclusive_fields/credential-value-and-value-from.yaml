apiVersion: lux.latere.ai/v1beta1
kind: Provider
metadata:
  name: p
spec:
  dialect: openai
  baseURL: https://api.example.com/v1
  credential:
    value: sk-example
    valueFrom:
      env: LUX_PROVIDER_P_CREDENTIAL
